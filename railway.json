{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "command": "pip install -r requirements.txt"
  },
  "deploy": {
    "command": "python backend/manage.py migrate && python backend/manage.py collectstatic --noinput && gunicorn backend.wsgi --chdir backend --bind 0.0.0.0:$PORT"
  }
}
