{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn backend.wsgi --chdir backend --bind 0.0.0.0:$PORT",
    "preDeploy": "pip install -r requirements.txt && python backend/manage.py migrate && python backend/manage.py collectstatic --noinput"
  }
}
